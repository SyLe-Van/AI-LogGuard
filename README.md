# AI-LogGuard Prototype

Prototype CLI tool for fetching and parsing logs from Jenkins and GitLab.

## Features
- Fetch logs from Jenkins and GitLab
- Parse logs using regex
- CLI interface

## Usage
Run CLI:
```
python src/main.py
```
